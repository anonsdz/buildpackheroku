{
  "name": "heroku-app",
  "version": "1.0.0",
  "description": "Ứng dụng Heroku với cloudflared và code-server",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "postinstall": "npm install -g node-addon-api && npm install -g cloudflared code-server"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {
    "node-addon-api": "^4.2.0"
  }
}
