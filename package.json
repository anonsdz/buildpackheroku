{
  "name": "heroku-app",
  "version": "1.0.0",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "postinstall": "npm install -g cloudflared && curl -fsSL https://code-server.dev/install.sh | sh"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
